# 音乐解析链改造方案

> 适用项目：音楽 - Music Downloader  
> 目标场景：朋友范围长期使用，存在一定并发，不收费，优先保证稳定、速度和可维护性  
> 改造原则：兼容现有客户端、分阶段上线、可以随时回退、第一阶段不引入付费服务

## 1. 改造目标

本次改造只集中处理“搜索结果拿到以后，如何快速、稳定地得到可播放链接”。搜索、登录、会员、资料库、支付和播放器 UI 暂不做无关重构。

核心目标：

- 普通情况下点击歌曲后 1～3 秒开始播放。
- 上游波动时尽量在 7 秒内给出成功结果或明确失败。
- 同一次解析最多并发请求 2 个上游，避免瞬间打满公共接口。
- 同一首歌的并发请求只执行一次实际解析。
- 解析来源由 Worker 统一选择，前端不再区分 `backup`、`backup3`、`backup4`。
- 保留现有接口作为兼容层，灰度期间可以立即切回旧逻辑。
- 暂不强依赖 Durable Object；只有实际规模需要时才启用。

建议上线后的验收指标：

| 指标 | 目标 |
| --- | --- |
| 解析成功率 | 不低于改造前，目标 95% 以上 |
| 首次解析 P50 | 2 秒以内 |
| 首次解析 P95 | 6 秒以内 |
| 单次平均上游解析调用 | 不高于 1.8 次 |
| 单次最大并发上游数 | 2 |
| 解析整体截止时间 | 7 秒，最长不超过 8 秒 |
| D1 同步写入等待 | 解析返回关键路径中为 0 |

## 2. 当前逻辑与主要问题

### 2.1 当前实际播放路径

前端 `ensureParsedSong()` 已直接抛出“主解析服务未启用”，因此普通搜索结果的主要播放路径实际上是：

```text
点击播放
  -> resolveBackup4Parsed()
  -> GET /api/proxy/backup4?mode=url
  -> prioritizeBackupChain()
  -> handleBackup4() 串行尝试 provider
  -> provider 返回 URL
  -> probeMediaUrl() 再次验证
  -> 返回前端
  -> /api/proxy/media 拉取音频
```

如果歌曲来自旧的 `backup` 或 `backup3` 搜索结果，前端又会绕过上述路径，分别走 `ensureBackupPlayableData()` 或 `ensureBackup3PlayableData()`。这使“歌曲是从哪里搜出来的”影响“歌曲如何播放”。

### 2.2 四层结构的历史包袱

现有四层大致对应：

1. 主解析：TuneHub。
2. `backup`：GDStudio。
3. `backup3`：雨糖 QQ 专用接口。
4. `backup4`：多 provider 聚合解析链。

现在 TuneHub 主解析已经停用，而 GDStudio、雨糖等又已被纳入 `backup4`，因此四层在职责上发生重叠。继续增加“第五层”只会进一步扩大维护成本。

### 2.3 造成延迟的具体原因

- Worker 对 7～9 个 provider 完全串行尝试。
- 通用 provider 超时约 6.5 秒，部分接口达到 8～12 秒。
- provider 返回 URL 后，外层还会执行一次最长 6 秒的 `probeMediaUrl()`。
- NXVAV、11NA 等 provider 内部已经执行 Range 探测，外层仍会重复探测。
- 前端允许 `/backup4` 等待 40 秒，用户可能长时间停留在加载状态。
- 每次 provider 成功或失败都会同步 `await recordServiceMetric()` 写 D1。
- 成功时还会继续写平台解析统计和最终来源统计。
- 现有排序只使用最近 24 小时聚合成功率和平均耗时；低流量时样本不足，连续失败和刚刚恢复无法及时反映。
- 健康 provider 会根据歌曲 ID 轮换，可能让较慢但健康的 provider 排在最快 provider 前面。

## 3. 目标架构

### 3.1 统一解析入口

新增统一接口：

```http
POST /api/proxy/resolve
Content-Type: application/json
```

请求：

```json
{
  "platform": "netease",
  "id": "123456",
  "quality": "320k",
  "name": "歌曲名",
  "artist": "歌手",
  "album": "专辑",
  "cover": "https://example.com/cover.jpg"
}
```

成功响应：

```json
{
  "code": 0,
  "message": "Success",
  "data": {
    "platform": "netease",
    "id": "123456",
    "quality": "320k",
    "url": "https://example.com/audio.mp3",
    "provider": "gdstudio",
    "lyrics": "",
    "cover": "",
    "cached": false,
    "elapsed_ms": 1380
  }
}
```

接口规则：

- 使用现有 Cookie/API Key 鉴权和音乐访问权限。
- `platform + id + quality` 是解析主键。
- `name`、`artist` 只用于需要关键词匹配的 provider。
- 不在服务端静默降低用户选择的音质。
- 如未来支持音质降级，必须在响应中返回实际音质并由前端提示。

旧接口 `/api/proxy/backup`、`backup3`、`backup4` 暂时保留，但新前端不再直接调用。

### 3.2 统一 provider 适配器

每个解析源只实现一个统一接口：

```js
{
  id: "gdstudio",
  platforms: ["netease", "kuwo"],
  configured(env) {},
  async resolve(input, context) {
    return {
      url: "https://...",
      provider: "gdstudio",
      lyrics: "",
      cover: "",
      validated: false
    };
  }
}
```

`context` 至少包含：

- `signal`：用于胜出后取消其他请求。
- `deadlineAt`：整体截止时间。
- `env`：Worker 配置。
- `requestId`：日志关联 ID，不包含用户隐私。

所有 provider 都必须遵守：

- 使用统一错误类型区分超时、限流、鉴权失败、无结果和链接失效。
- 接收外部 AbortSignal，不再各自创建无法取消的孤立请求。
- 缺少 Secret 或不支持当前平台时直接标记不可用，不进入候选队列。
- 返回 `validated: true` 的 provider 不再执行重复媒体探测。

### 3.3 解析调度器

解析调度器负责 provider 筛选、排序、延迟竞速、熔断、缓存和统计，provider 自身不决定调用顺序。

建议的单次调度流程：

```text
1. 检查成功缓存
2. 检查当前实例是否已有相同解析任务
3. 过滤不支持、未配置、管理员禁用、熔断中的 provider
4. 按健康评分排序
5. 启动第 1 名
6. 700ms 尚未成功，启动第 2 名
7. 第一轮失败，启动第 3、4 名，最多同时 2 个
8. 第一个通过验证的结果胜出
9. 取消其他请求
10. 写成功缓存并异步记录指标
11. 超过 7 秒统一结束
```

不建议一次性并发所有 provider。

### 3.4 provider 分组

每个平台不再维护固定的绝对顺序，而是维护基础优先级；实时健康度在基础优先级上动态调整。

初始可按当前经验分成：

- 快速组：配置完整、历史成功率高、直接按歌曲 ID 解析的来源。
- 标准组：可用但速度或稳定性一般的来源。
- 救援组：依赖关键词匹配、接口不稳定或耗时较长的来源。

救援组只有在前两组失败时进入，不参与日常竞速。管理员禁用功能继续保留。

## 4. 智能排序与熔断

### 4.1 排序数据

每个 provider、每个平台分别维护：

- 最近请求数。
- 最近成功数。
- 最近 10～20 次的滚动成功率。
- 平均耗时和近期高延迟情况。
- 连续失败次数。
- 最近成功时间。
- 最近失败时间。
- 最近一次 429、401、403 状态。
- 熔断截止时间。

第一版不需要机器学习，可以使用可解释的评分：

```text
评分 = 成功率奖励
     - 响应耗时惩罚
     - 连续失败惩罚
     - 限流/鉴权错误惩罚
     + 最近成功奖励
     + 当前歌曲上次成功来源奖励
```

排序应以可靠性优先，在可靠性接近时选择更快的来源。不要再仅为了平均分流而把慢源轮换到第一位。

### 4.2 熔断规则

建议初始规则：

| 情况 | 动作 |
| --- | --- |
| 连续失败 2 次 | 降到当前平台队尾 |
| 连续失败 3 次 | 熔断 5 分钟 |
| 连续失败 5 次 | 熔断 30 分钟 |
| 返回 429 | 至少熔断 10 分钟，尊重 `Retry-After` |
| 返回 401/403 | 当作配置错误，熔断 1 小时并在管理页提示 |
| 普通超时 | 记一次失败，但惩罚低于 401/403 |
| 熔断到期 | 进入半开状态，只允许一个探测请求 |
| 半开成功 | 清零连续失败并逐步恢复 |

实例内状态先保存在内存中，D1 只保存聚合结果。无需为了每一次状态变化同步写数据库。

## 5. 缓存与请求合并

### 5.1 Worker 成功缓存

缓存键：

```text
resolve:v2:{platform}:{id}:{quality}
```

建议初始 TTL：

- 普通解析链接：120 秒。
- 已知较稳定链接：最多 300 秒。
- 不清楚有效期的签名链接：60～120 秒。
- 失败结果：15 秒，只缓存“暂时不可用”，避免故障风暴。

缓存响应必须带上 `provider`、`created_at` 和 `expires_at`。缓存命中后可以直接返回；如果媒体播放失败，前端使用 `bypass_cache: true` 强制重新解析一次。

第一版使用 Cache API，不把短期播放链接写入 D1。

### 5.2 实例内请求合并

维护：

```js
const inflightResolves = new Map();
```

同一缓存键已有 Promise 时，后续请求直接等待该 Promise。任务完成后必须在 `finally` 删除，避免内存泄漏。

这可以解决同一 Worker 实例中多人同时播放热门歌曲导致的重复解析。实例销毁后状态自然释放。

### 5.3 Durable Object 的使用条件

第一阶段不使用 Durable Object。满足以下任一条件后再评估：

- 实例内合并命中率很低，但上游仍有大量相同歌曲重复解析。
- 需要跨地区严格限制某个 provider 的全局并发。
- 需要严格的全局 API Key 限流。
- 日志显示跨实例重复解析已成为主要压力来源。

届时优先使用 SQLite-backed Durable Object，并按歌曲哈希或 provider 分片，避免所有请求集中到单个对象。

## 6. 媒体链接验证

### 6.1 避免重复探测

provider 返回以下任一情况时可标记 `validated: true`：

- provider 已通过 Range 请求拿到音频响应。
- provider 接口本身直接返回音频流且已检查 Content-Type/Content-Range。
- 已知该 provider 返回前已经完成有效性验证。

只有 `validated: false` 的链接才执行统一探测。

### 6.2 统一探测规则

- 使用 `Range: bytes=0-1023`。
- 单次探测超时建议 1500～2000 毫秒。
- 接受合理的 `audio/*` 和部分 CDN 的 `application/octet-stream`。
- 根据 Content-Range 或 Content-Length 过滤明显的试听片段和错误页面。
- 验证请求必须使用调度器传入的 AbortSignal。
- 不能因为缺少 Content-Length 就直接判定失败。

## 7. D1 监控改造

### 7.1 从关键路径移除

Worker 入口改为：

```js
export default {
  async fetch(request, env, ctx) {
    return handleRequest(request, env, ctx);
  }
};
```

解析结果先返回，指标写入通过：

```js
ctx.waitUntil(flushMetrics(...));
```

执行。指标写入失败不能影响播放。

### 7.2 聚合写入

避免每次 provider 调用都更新 D1 同一小时桶：

- 单次解析产生的多个指标先组成一个批次。
- 使用一次 `db.batch()` 写入。
- 清理 30 天旧数据不再随机阻塞用户请求，改为定时任务或独立异步任务。
- API Key 的 `last_used_at` 不必每个请求都写，可按 5～15 分钟节流。

管理页现有统计结构可以继续使用，不需要先重做界面。

## 8. 并发保护与限流

### 8.1 provider 并发舱

先实现单 Worker 实例内的软并发限制：

| provider 类型 | 建议并发上限 |
| --- | --- |
| 自己可控或稳定配置源 | 5～10 |
| 普通公共接口 | 2～3 |
| 实验性/救援接口 | 1 |

达到上限时调度器跳过该 provider，不在其后长时间排队。未来如需全球严格限制，再迁移到 Durable Object。

### 8.2 API Key 限流

初始建议：

- 搜索：每个 Key 每分钟 30 次。
- 解析：每个 Key 每分钟 15 次，允许短时突发 5 次。
- `/api/topone`：每个 Key 每分钟 15 次。
- Cookie 网页会话采用账号维度加 IP 辅助限流。
- 媒体 Range 请求不能简单按请求次数限制，否则拖动进度会误伤。

第一版可以是近似限流，以防客户端死循环。需要严格全局限流时再使用 Durable Object。

## 9. 前端改造

### 9.1 统一播放逻辑

前端新增：

```js
resolveSongMedia(song, quality, options)
```

所有 `primary`、`backup`、`backup3`、`backup4` 搜索结果都调用统一 `/resolve` 接口。前端歌曲对象只保留：

- `platform`
- `id`
- `name`
- `artist`
- `album`
- `cover`
- `lookupOnly`

旧的 `backup`、`backup3` 元数据先保留用于读取历史 localStorage，加载后转换为统一歌曲对象。新保存的数据不再写入 provider 专属结构。

### 9.2 播放失败重试

播放步骤：

1. 正常调用 `/resolve`。
2. 将 URL 交给 `/media` 播放。
3. `audio.play()` 或媒体加载失败时，只允许一次 `bypass_cache: true` 重解析。
4. 第二次失败后明确提示，不无限重试。

### 9.3 预加载

保留下一首预解析，但调整为：

- 当前歌曲已经稳定播放后再启动。
- 使用统一 `/resolve`。
- 不主动完整加载媒体文件，只预解析 URL 或读取极小 Range。
- 网络节省模式、2G 网络继续禁用。
- 预加载请求优先级低于用户主动点击。

## 10. `/api/topone` 改造

`topone` 保留跨平台并发搜索，但解析候选时改为直接调用统一解析调度器，不再通过构造内部 Request 间接调用 `handleBackup4()`。

建议预算：

- 搜索预算：2.5～3 秒。
- 解析预算：剩余 3.5～4 秒。
- 整体仍控制在约 6 秒。
- 平台可以并发搜索，但每个平台不要各自启动完整的多 provider 串行链。
- 候选歌曲最多尝试 2～3 首，优先标题和歌手匹配度最高的结果。

`topone` 与网页解析共享成功缓存和 provider 健康状态。

## 11. 分阶段实施计划

### 阶段 A：建立基线与安全开关

工作内容：

- 为解析响应增加 `request_id`、`elapsed_ms`、`attempt_count`、`provider`。
- 管理统计增加解析 P50/P95、缓存命中率和平均尝试数。
- 新增环境变量开关：
  - `RESOLVER_V2_ENABLED=false`
  - `RESOLVER_V2_PERCENT=0`
  - `RESOLVER_TOTAL_BUDGET_MS=7000`
  - `RESOLVER_HEDGE_DELAY_MS=700`
  - `RESOLVER_MAX_ATTEMPTS=4`
  - `RESOLVER_MAX_CONCURRENCY=2`
- 保持所有用户继续走旧路径。

验收：能看到改造前真实延迟和成功率，不改变播放行为。

### 阶段 B：先做无风险性能优化

工作内容：

- D1 指标改为 `ctx.waitUntil()` 异步批量写入。
- API Key `last_used_at` 写入节流。
- 清理任务移出用户响应路径。
- 去除已验证 provider 的重复媒体探测。
- 将探测超时降至 2 秒左右。

验收：解析结果与旧版一致，等待时间下降，D1 错误不影响播放。

### 阶段 C：缓存与请求合并

工作内容：

- 增加 Worker Cache API 成功/短期失败缓存。
- 增加实例内 `inflightResolves`。
- 播放失败支持一次绕过缓存重解析。
- 增加缓存命中和合并命中统计。

验收：同一歌曲 20 个并发请求只触发一套主要上游解析；缓存命中不重复调用 provider。

### 阶段 D：统一解析调度器

工作内容：

- 将现有 provider 提取为适配器。
- 实现评分、熔断、半开恢复、延迟竞速和 AbortController。
- 新增 `/api/proxy/resolve`。
- 保留旧 `/backup*` 路由。
- 先只让内部测试账号或固定比例请求使用 V2。

灰度顺序：

```text
0% -> 管理员测试 -> 10% -> 30% -> 60% -> 100%
```

每一步至少观察一个完整使用高峰；成功率下降或上游调用数明显增加时立即回退。

### 阶段 E：前端统一

工作内容：

- 所有播放和下载改用 `/resolve`。
- 迁移历史 localStorage 歌曲结构。
- 删除前端对 `backup`、`backup3`、`backup4` 的运行时分支。
- 更新 API 和部署文档。

验收：同一首歌无论从哪个搜索源出现，都走完全相同的播放路径。

### 阶段 F：并发保护

工作内容：

- 加 provider 实例内并发舱。
- 加 API Key 近似限流。
- 根据实际日志决定是否引入 Durable Object。

这一阶段结束后，再删除确认无人调用的旧接口和前端兼容代码。

## 12. 测试计划

### 12.1 单元测试

- 每个 provider 的响应解析。
- provider 配置缺失时是否正确跳过。
- 健康评分排序。
- 连续失败熔断和半开恢复。
- 429、401、403、超时分类。
- 音频链接验证和试听片段判断。
- 缓存键及 TTL。
- 历史歌曲对象迁移。

### 12.2 调度测试

- 第一源 200ms 成功：不启动第二源。
- 第一源超过 700ms：启动第二源。
- 第二源先成功：取消第一源。
- 第一组失败：第二组启动。
- 超过总预算：所有请求取消并返回统一错误。
- 同一首歌 20 个并发请求：实际解析只执行一次。
- 不同歌曲并发：互不阻塞。
- provider 达到并发上限：自动跳过而不是排队。

### 12.3 路由兼容测试

- Cookie 登录。
- API Key + 米家 UID +设备令牌。
- 月会员限制。
- 管理员绕过。
- `/topone` Bearer Key。
- 旧 `/backup*` 路由仍可调用。
- 媒体 Range 和下载文件名。

### 12.4 压力测试

至少模拟：

- 50 个并发请求同一歌曲。
- 50 个并发请求不同歌曲。
- 一个 provider 全部超时。
- 一个 provider 持续返回 429。
- D1 暂时不可用。
- Cache API 未命中或异常。

关注解析延迟、实际上游调用数、D1 写入数和 Worker 错误率。

## 13. 回退方案

- V2 全程受 `RESOLVER_V2_ENABLED` 和百分比开关控制。
- 前端上线初期保留旧解析函数。
- `/resolve` 失败时是否回退旧 `/backup4` 由灰度阶段决定；正式 100% 上线后不再双重回退，避免一次请求触发两套完整链路。
- 数据库只新增统计字段或表，不破坏现有表。
- 旧接口至少保留一个稳定观察周期后再删除。

## 14. 本次不做的事情

- 不引入机器学习模型。
- 不一次性删除所有旧接口。
- 不默认启用 Durable Object。
- 不同时并发全部解析源。
- 不把短期播放 URL 持久化到 D1。
- 不无提示降低音质。
- 不借此次改造重写整个前端或认证系统。

## 15. 推荐的实际执行顺序

最优先执行：

1. D1 统计异步化和批量化。
2. 删除重复媒体探测并缩短验证超时。
3. Worker 短期缓存。
4. 相同歌曲请求合并。
5. 新统一 `/resolve` 调度器。
6. 两源延迟竞速、熔断和取消。
7. 前端统一播放路径。
8. 限流和 provider 并发舱。
9. 根据真实压力决定 Durable Object。

按照这个顺序，每一步都有独立收益，也都可以单独验证和回退。

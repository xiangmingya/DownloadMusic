# 小爱音箱 × QNAP 实时点歌 · 集成方案与协议（草案）

> 状态：M1 ✅ / M2 ✅ 真机验证通过 / M3 进行中（Worker 已加 topone，待智能音箱插件联调）
> 版本：v0.3 · 2026-08-08
> 适用范围：现有「音楽 - Music Downloader」项目（Cloudflare Worker + 静态前端）接入小米 AI 音箱，通过 QNAP NAS 提供语音实时点歌能力。

---

## 1. 背景与目标

- 现有项目是一个音乐下载器：前端（web/）+ Cloudflare Worker（worker/），负责登录鉴权、搜索（网易/QQ/酷我）、播放链接解析、媒体代理。
- 目标：用户对着小爱音箱说「播放 XXX」就能实时听到歌，**不需要先下载到本地**。
- 目标设备：小米 AI 音箱（一代，S12）+ QNAP 威联通 NAS（Docker）。
- 商业形态（未来）：用户在自己 QNAP 上运行轻量客户端，填写 API Key 即可使用解析服务；KEY 目前不做收费，但协议要为将来的版权 / 商业化留好锚点。

---

## 2. 总体架构

```text
网页端(用户弹窗 → 对话框) ──申请/查看 KEY──▶ 云端 Worker
                                              │  X-DM-Key + X-DM-Device-Id 校验
QNAP 客户端(Docker) ────────带 KEY 调接口────▶ 搜索 / 解析 / 拉流
   │
   ├─ 米家扫码绑定小爱音箱
   └─ 语音「播放XXX」→ 解析 → 转流(LAN URL) → 音箱播放
```

核心原则：

1. **解析能力留在云端**，KEY 只做授权；用户手里不持有 TuneHub Key 或解析逻辑。
2. 音频走 NAS 局域网地址转流（`player_play_url`），**不需要公网隧道**。
3. 现在不做配额和收费，但字段、接口、鉴权位置先留好，等拿到版权再开启。

---

## 3. 语音通道选型（待真机验证）

| 方案 | 机制 | 优点 | 风险/代价 |
| --- | --- | --- | --- |
| MIoT 对话监听（主推） | 客户端登录米家账号，监听音箱对话并抢答，调用 `player_play_url` 播放 | 无需小爱开放平台、无需技能审核、多用户分发友好 | 小米 AI 音箱一代兼容性需真机实测；米家账号自动化有封号风险 |
| 小爱自定义技能（兜底） | 小爱开放平台注册开发者，配置 HTTPS 回调，用户说「用XXX播放…」 | 官方能力，一代音箱支持确定 | 需实名注册 + 技能审核，音乐类易被拒；只适合单机/自己用 |

结论：产品主线走 **MIoT 对话监听**；自定义技能只做单机兜底。二者都不影响 M1（KEY 体系）的落地。

---

## 4. API Key 协议（M1）

### 4.1 归属与数量

- 密码登录：家庭共享一套 Key（owner_key = `password:family`）。
- Linux DO 登录：每个用户独立一套 Key（owner_key = `linuxdo:<id>`）。
- **一个账号只有一个 Key**，不做多 Key 列表。

### 4.2 三层绑定（暂时 1 台设备）

设备绑定身份使用**米家账号 ID**（客户端米家扫码后从小米云获取），并叠加服务端签发的**设备令牌**，防止 Key 被复制分享：

- `X-DM-Key`：证明「你是这个账号」（网页端申请）。
- `X-DM-Mi-Uid`：证明「你是这个小微账号」（米家扫码后获取）。
- `X-DM-Device-Token`：证明「你是这台设备」（保存绑定时服务端签发，客户端保存）。
- 客户端在容器首次启动时生成固定设备 ID（`X-DM-Device-Id`）存本地，重启不丢。
- **保存即绑定**：用户在客户端点「保存」时调 `/api/client/bind`，服务端校验 Key + 米家 UID + 设备 ID，通过后记录绑定并签发设备令牌；已绑定时幂等返回原令牌。
- 换设备 / 换米家账号：网页端点「解绑设备」清除绑定与令牌；未解绑时换账号或换设备调用返回 `403`。

说明：此模型防止「把 Key 字符串复制给别人用」；若对方完整克隆客户端配置（Key + 令牌 + 米家会话），则超出本层防护，属设备被入侵范畴。

### 4.3 鉴权方式

- 客户端请求头：
  - `X-DM-Key`：API Key（必填）
  - `X-DM-Mi-Uid`：米家账号 ID（必填，代理接口与绑定接口）
  - `X-DM-Device-Id`：设备唯一 ID（必填，参与绑定）
  - `X-DM-Device-Token`：设备令牌（必填，代理接口；由绑定接口签发）
  - `X-DM-Device-Name`：设备显示名（可选）
- 分接口校验：
  - `/api/auth/me`：**只验 Key**（客户端「测试按钮」用，不触发绑定）。
  - `/api/client/bind`：Key + 米家 UID + 设备 ID（保存绑定时调用）。
  - `/api/proxy/*`：Key + 米家 UID + 设备令牌三层校验；无 Key 头时回退原 Cookie 会话（网页端不变）。
- Key 持有者视为已授权客户端（当前策略：**绕过会员限制**，等价于管理员级服务访问，后续可收口）。

### 4.4 接口定义

#### 4.4.1 客户端接口（带 X-DM-Key）

| 接口 | 方法 | 说明 |
| --- | --- | --- |
| `/api/auth/me` | GET | 只验 Key：连通性 + 鉴权状态检查（「测试按钮」用），不触发绑定 |
| `/api/client/bind` | POST | 保存绑定：Key + 米家 UID + 设备 ID → 返回设备令牌；幂等 |
| `/api/proxy/*` | GET/POST | 搜索、解析、播放链接、媒体代理等，三层校验 |

#### 4.4.2 KEY 管理接口（网页 Cookie 登录，服务端校验归属）

| 接口 | 方法 | 说明 |
| --- | --- | --- |
| `GET /api/keys` | GET | 查询自己的 Key 状态（是否存在、完整 Key、绑定小米账号） |
| `POST /api/keys` | POST | 申请 Key；已有则返回错误。成功时明文只返回一次 |
| `POST /api/keys/reset` | POST | 重新生成：旧 Key 作废并解绑，返回新 Key 明文 |
| `POST /api/keys/unbind` | POST | 解绑设备（同时作废设备令牌） |

### 4.5 错误码

| HTTP | code | 场景 |
| --- | --- | --- |
| 401 | 401 | Key 无效 / 不存在 / 已过期 / 未登录 |
| 403 | 403 | 该 Key 已绑定其他小米账号 / 其他设备 |
| 400 | -1 | 参数错误（如重复申请） |
| 404 | 404 | 要撤销/解绑的记录不存在 |
| 503 | 503 | D1 数据库未配置 |

### 4.6 数据存储（当前决策：明文）

- 为满足「打开就能看到、随时能复制」的交互，**当前直接存 Key 明文**（与访问密码同级保护，家庭场景足够）。
- 商业化时切换为哈希存储（SHA-256），接口与界面不变，属于未来锚点。

### 4.7 数据表设计（api_keys）

```sql
CREATE TABLE IF NOT EXISTS api_keys (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  owner_key TEXT NOT NULL UNIQUE,          -- password:family / linuxdo:<id>
  api_key TEXT NOT NULL,                   -- 当前明文；未来改存哈希
  created_at TEXT NOT NULL,
  last_used_at TEXT,
  mi_uid TEXT,
  device_id TEXT,
  device_name TEXT,
  device_token TEXT,
  bound_at TEXT,
  enabled INTEGER NOT NULL DEFAULT 1,      -- 锚点：将来可停用
  expires_at TEXT                          -- 锚点：将来可设过期
);
CREATE INDEX IF NOT EXISTS idx_api_keys_owner ON api_keys (owner_key);
```

---

## 5. 网页端交互（M1）

- 入口：顶部导航「用户」弹窗内新增一行「API Key 接入」，点击打开独立小对话框（保持弹窗轻量，一个窗口即可）。
- 对话框内容（就一个输入框 + 一个主按钮 + 一行状态）：
  1. 没申请过：输入框为空，按钮显示「申请」；点击后生成 Key 并明文填入输入框，按钮变为「复制」。
  2. 申请过：打开即显示 Key，按钮直接是「复制」。
  3. 底部小字：已绑定小米账号（尾号 xxxx）+「解绑设备」；另有「重新生成」文字链接（旧 Key 作废、发新 Key）。
- 明文 Key 仅前端展示；前端不做 Key 校验逻辑，全部由 Worker 完成。

---

## 6. QNAP 客户端设计（M3：放弃自研插件，直连智能音箱插件外部搜索源）

### 6.1 形态

不再自研 QNAP 容器或 JS 插件，直接使用 Songloft「智能音箱」插件自带的**外部搜索源**配置：

- 智能音箱插件负责：米家扫码登录、对话监听、播放控制、多房间同步（已装好、已验证）。
- 我们的 Worker 新增 `POST /api/topone`（topone 规范），只负责「按歌名找播放地址」。
- 配置方式（智能音箱插件 → 设置 → 外部搜索）：
  - 添加一个搜索源：名称任意，地址填 `https://musicapi.621888.xyz/api/topone`，Token 填 API Key（KEY）。
  - 自带的**测试按钮**直接验证连通性；多个源按列表顺序优先级，天然实现「指定音源 / 优先音源」。
  - 指定音源：只保留一个源并加 `?platform=netease`（或 qq / kuwo）；优先音源：多个源按序排列。
- 省掉的层：不写插件、不打 zip、不装第二个插件；KEY 验证保留（只验 Key），设备绑定留作后续锚点（届时再补品牌设置页 / 绑定）。

### 6.2 播放链路

```text
用户语音 → 智能音箱插件对话监听
  → 本地曲库未命中 → 调外部搜索源 Worker /api/topone（Bearer KEY）
  → Worker 按平台优先级搜索 + 解析出直链 URL
  → 智能音箱插件 player_play_url 推送 → 音箱播放
```

- 全程 1~3 秒，无需先下载落盘；CDN 直链对音箱可达（与内置 ytdlp / bili 搜索源同机制）。
- 上游 CDN 链接短时有效；若后续需要更稳，可加 NAS 局域网中转（LAN URL 已真机验证可行）。

### 6.3 音源策略语义

- **指定音源**：只用所选平台（网易/QQ/酷我），解析失败即明确报错，不做回退。
- **优先音源**：按用户排序依次尝试（网易失败换 QQ，再换酷我），全部失败才报错。
- 实现位置：智能音箱插件的**外部搜索源列表顺序**即优先级（源 0 命中即用，未命中试源 1…）；平台顺序在 Worker 端由 `platform` 查询参数控制（缺省 `netease,qq,kuwo`）。

---

## 7. 验证步骤（M2 已完成）

1. ✅ QNAP 部署 Songloft（Container Station），米家扫码绑定小爱音箱。
   - ✅ 对话监听：语音「播放TEST」命中本地文件 `TEST.mp3`，`[MinaClient] play-url ubus success`。
   - ✅ LAN URL 播放：`player_play_url` 播放 NAS 本地地址成功；「暂停」等固定口令也通。
2. Worker 加 KEY：`/api/auth/me`、搜索、解析均带 Key 可通（M1 冒烟测试通过）。
3. ✅ 网页端生成 Key（`xm_c1cf…`）→ 测试按钮通过 → 音箱实际播放本地歌曲。
4. ⏳ M3 联调：智能音箱插件填 topone 地址 + KEY → 测试按钮 → 语音「播放XXX」走外部搜索源出声。

---

## 8. 里程碑

| 阶段 | 内容 | 前置条件 |
| --- | --- | --- |
| M1 | Worker KEY 体系 + 网页端管理 | ✅ 已完成（已部署：`X-DM-Key` 鉴权、`/api/client/bind` 三层绑定、4 个管理接口、网页端对话框） |
| M2 | 真机验证语音通道（对话监听 + LAN URL 播放） | ✅ 已完成（S12A 实测：`TEST.mp3` 语音点播 + 暂停口令 + `play-url ubus success`） |
| M3 | Worker `topone` 接口 + 智能音箱插件外部搜索源配置（KEY / 音源顺序 / 测试按钮用插件自带） | M2 验证结果 |
| M4 | 商业化开关（配额 / 定价 / 风控 / 哈希存储） | 拿到版权后 |

---

## 9. 风险与开放问题

- ~~最大风险：小米 AI 音箱一代「对话监听」兼容性未验证~~ → ✅ 已真机排除（S12A 实测可用）。
- 智能音箱插件版本较旧时外部搜索配置可能不完整，联调前先确认插件已更新到最新版。
- 米家账号自动化存在被小米风控封号的可能。
- 解析版权音乐做收费分发有法律风险，商业化前需确认版权。
- 三层绑定（Key + 米家 UID + 设备令牌）可防「Key 字符串被复制分享」；但无法防「整个客户端配置被克隆」，该场景需另行加固。
- 明文存储 Key 的安全取舍：家庭场景可接受，商业化前切哈希。

---

## 10. 未来扩展锚点

- `api_keys.enabled`：Key 停用/封禁。
- `api_keys.expires_at`：Key 过期。
- 配额字段（每日解析次数、流量）、限流、速率控制。
- KEY 与米家账号 / 设备绑定（真限制）。
- API 侧网络活动提醒（当前采用）：记录 Key 的脱敏网络段、地区和运营商；多网络异常只在会员管理端提醒，由管理员人工处理，不自动封禁。
- 哈希存储 + 明文仅一次展示。
- 签名媒体 URL（`sig` + `exp`，HMAC 可携带 KEY ID）——小爱云端直接拉流时使用。

---

## 附录 A：小爱自定义技能音频协议（兜底方案参考）

来自小爱开放平台官方文档（https://developers.xiaoai.mi.com ），自定义技能 HTTPS 回复中播放音频的动作格式：

```json
{
  "version": "1.0",
  "response": {
    "open_mic": false,
    "directives": [
      {
        "type": "audio",
        "audio_item": {
          "stream": {
            "url": "https://example.com/audio.mp3"
          }
        }
      }
    ]
  },
  "is_session_end": false
}
```

- 小爱平台 POST 到开发者 HTTPS 服务，请求体含 `query`（用户语音识别结果）与 `slot_info`（意图/槽位）。
- 显式唤醒：用户话术必须包含技能调用名称，如「用XXX播放周杰伦的晴天」。
- 音频 URL 需公网 HTTPS 可达（小爱云端拉流，拿不到调用方 Cookie）。
